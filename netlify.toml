# ══════════════════════════════════════
# Netlify Configuration
# ══════════════════════════════════════

[build]
  publish = "."

# ── Edge Functions (30s timeout vs 10s for regular functions) ──
[[edge_functions]]
  function = "gemini"
  path = "/api/gemini"
